# Copy this file to .env and fill in real values for production.

# Django
DJANGO_SECRET_KEY=change-this-to-a-long-random-string
DJANGO_DEBUG=False

# Database (leave DB_ENGINE unset to use local SQLite)
DB_ENGINE=postgresql
DB_NAME=srikrishna
DB_USER=srikrishna
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Shop payment / notification settings are configured inside the app
# (Settings page) rather than environment variables: UPI ID, WhatsApp
# number, GSTIN, invoice prefix, etc.
